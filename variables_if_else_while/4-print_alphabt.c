#include <stdio.h>
/**
 * main - entry point
 *
 * Description: prints every letter in the alphabet excerpt e and q, and then prints a new line.
 *
 * Return: 0
 */
int main(void)
{
	char l;

	for (l = 'a'; l <= 'z'; l++)
	{
		if (l2 != 'q' && l2 != 'e')
			putchar(l);
	}
	putchar('\n');

	return (0);
}
