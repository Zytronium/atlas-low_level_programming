#include "main.h"
/**
 * cap_string - capitalizes the first letter of all words.
 * @s: what
 * Return: 0
 */
char *cap_string(char *s)
{
	int a = 0;
	int i;

	for (i = 0; s[i]; i++)
		if()

	return (s);
}
